# 이 저장소에서 작업할 때

내레이션 기반 faceless 영상을 한 편씩 만드는 공정입니다.
사람은 세 곳에서만 판단합니다 — **대본 / 화면 첫 렌더 / 업로드**.

## 먼저 읽을 것

| 무엇을 하려는가 | 읽을 문서 |
|---|---|
| 한 편을 처음부터 만든다 | `skills/knowhow-episode/SKILL.md` — **제작 런북. 단계·판단 기준·통과 기준** |
| 대본을 쓴다 | `docs/SCRIPT_FORMAT.md` — `[V]` `[N]` 태그와 규칙, 씬 길이 기준 |
| 음성 서비스를 고르거나 바꾼다 | `docs/VOICE_PROVIDERS.md` |
| 목소리를 녹음하거나 클론한다 | `docs/RECORDING.md` |
| 화면 소재를 구하고 넣는다 — 녹화, AI 생성, 손그림, 기존 파일 | `docs/VISUALS.md` (손그림 도구는 선택 설치: `pipeline/install_whiteboard.sh`) |
| 화면 카드 종류를 본다 | `remotion/README.md` |
| **화면 생김새를 바꾼다** — 색, 글꼴, 글자 크기, 여백, 모서리, 자막 위치·크기, 카드 배치, 전체 톤 | `docs/DESIGN.md` — 어느 층을 고칠지 먼저 판단한다 |
| 어떤 스크립트가 무엇을 하나 | `pipeline/README.md` — 단계별 입출력 표 |
| 설치가 됐는지 본다 | `python3 pipeline/check_setup.py` |

**새 편을 만들거나 기존 편을 고칠 때는 런북을 먼저 읽으세요.** 나머지는 필요할 때 펴 보면 됩니다.

## 한 편의 흐름

```
00_new_episode.sh   에피소드 폴더 + 대본 템플릿
  ↓  사람이 script/script_v1.md 를 쓴다  ← 승인 1
05_script_to_scenes 대본 → 씬 JSON
10_tts_prep         숫자·영문을 음성용 한글 읽기로
20_tts_generate     씬별 음성 생성
30_nar_check        받아쓰기로 검사 (문장 누락·꼬리 잡음)
35_nar_retry        걸린 씬만 다시
40_nar_finalize     트림·정규화·씬 시각표
45_visual_plan      씬별 화면 계획서 (카드·이유는 대본을 읽고 채운다)
50_captions_build   자막 (원문 문장 + 받아쓰기 타이밍)
  ↓  사람이 화면 계획과 첫 렌더를 본다  ← 승인 2
55_remotion_sync → 60_render_master
  ↓  최종 편집자가 완성본을 본다 (보는 사람의 눈으로 · 아무것도 안 고친다)
65_render_derived → 70_srt_build → 75_chapters
85_ship_gate        관문 — 요청 대장이 다 「재서 확인됨」인가 · 그 지문이 이 마스터인가 ·
                    이 마스터를 편집자가 봤는가. **셋 중 하나라도 아니면 여기서 멈춥니다**
  ↓  사람이 올린다  ← 승인 3
```

## 지켜야 할 것

- **승인 없이 다음 단계로 넘어가지 않습니다.** 위 흐름의 세 지점입니다.
- **관문은 총괄을 막습니다.** `85_ship_gate.py` 는 담당이 아니라 **미루기로 한 사람**을 막습니다 —
  E01 에서 최종 편집자를 다섯 판 미룬 것과 사용자 요청을 표 없이 기억으로 관리한 것이 둘 다 총괄 자리였습니다.
  **마음먹으면 건너뛸 수 있는 것은 시스템이 아닙니다.**
- **사용자 요청은 `script/requests.json` 에 적습니다.** 「들어감」과 **「재서 확인됨」**을 가릅니다 —
  앞엣것은 코드에 넣은 것이고 뒤엣것은 **그 마스터에서 재 본 것**입니다. `07_requests_check.py` 가 근거를 봅니다.
- **한 항목이 아직이라고 검수 전체를 미루지 않습니다.** 그 항목만 판정에서 빼고 나머지를 보게 합니다 —
  E01 에서 BGM 하나 때문에 화면·문구·끝맺음이 같이 미뤄졌습니다.
- **요청받은 단계만 합니다.** 범위를 넓히거나 다른 단계를 먼저 하지 않습니다.
- **`scenes_v1.json` 을 손으로 만들지 마세요.** 대본은 `script/script_v1.md` 에 쓰고 `05_script_to_scenes.py` 로 변환합니다. 형식은 `docs/SCRIPT_FORMAT.md`.
- **상수는 `pipeline/common.py` 한 곳에 있습니다.** 스크립트마다 값을 다시 쓰지 마세요.
- **목소리 설정(`pipeline/voice.json`)은 잠금입니다.** 제공자·노드·참조 파일·생성 파라미터 등 노드 inputs 로 들어가는 값을 바꾸면 목소리가 바뀝니다. 재생성이 필요하면 **시드만** 바꿉니다 — `20_tts_generate.py <EP> --seed N`, 또는 `35_nar_retry.py <EP> --ids <씬> --tries N`(`retry_seeds` 를 차례로 씁니다).
- **음성 제공자는 `voice.json` 의 `provider` 하나로 정해집니다.** 새 서비스는 `pipeline/providers/` 에 어댑터를 넣거나 `shell` 제공자로 붙입니다. 공정의 다른 단계는 손대지 않습니다.
- **생성물은 보여주고 승인받은 뒤** 자산으로 등록하거나 다음 생성에 물립니다.
- **결함이 하나면 재생성하지 말고 그것만 고칩니다.** 재생성하면 맞던 디테일이 사라집니다.
- **생성 작업 전에는 항상 연결을 확인하고 계획을 세웁니다.** `01_status.py <EP>` 로 서버 상태·큐·남은 작업·예상 시간을 먼저 봅니다. 20·35 단계는 이 확인을 안에 포함하고 있습니다. 죽은 서버에 던져 놓고 기다리는 것이 가장 흔한 시간 낭비입니다.
- **씬별 화면은 계획서를 세우고 정합니다.** `45_visual_plan.py` 로 표를 뽑고, 대본을 읽어 카드와 이유를 채우고, 승인받은 뒤 `scenes.tsx` 를 씁니다. 즉흥으로 정하면 같은 카드가 연속되거나 증거를 그림으로 덮습니다.
- **테마(색·글꼴)는 편마다 바꾸지 않습니다.** 채널의 얼굴이라 편마다 달라지면 같은 채널로 안 읽힙니다. 내용에 따라 정하는 것은 씬별 카드 선택입니다.
- **화면 생김새를 바꿀 때는 `remotion/src/knowhow/theme.ts` 를 먼저 봅니다.** 색·글꼴·글자 크기·여백·모서리·자막 위치는 전부 거기 토큰으로 있습니다. "자막을 키워 달라", "톤을 밝게", "여백을 넓게" 같은 요청은 대부분 토큰 한두 개를 바꾸면 끝납니다. **컴포넌트 파일에 숫자를 직접 쓰지 마세요** — 한 번은 되지만 다음에 톤을 바꿀 때 파일을 다시 뒤지게 됩니다. 컴포넌트를 고쳐야만 하는 경우(배치·동작 변경)에도 새로 생기는 값은 토큰으로 빼세요. → `docs/DESIGN.md`
- **매번 손으로 하는 일이 생기면 스크립트로 만드세요.** 문서에 적어 둔 규칙은 잊히지만 공정 안에 있으면 어길 수가 없습니다.

## 경로 규약

```
assets/     여러 편에서 쓰는 것 — 로고·인트로·생성 참조·BGM (편마다 바뀌면 안 되는 것)
episodes/<PREFIX>_<slug>/
  source/   이 편에서만 쓰는 소재 + SOURCES.md (출처 기록)
  script/   script_v1.md (사람이 씀) → scenes_v1.json → scenes_v2.json, captions.json, chapters.json
  audio/    nar_raw/ (생성 원본), narration_final/ (트림·정규화본)
  edit/     마스터·프리뷰·썸네일·자막·챕터
```

Remotion 컴포지션 이름: `<PREFIX>-Episode`, `<PREFIX>-Thumb-A/B/C`, `<PREFIX>-Shorts-1/2`.
Remotion 위치는 `REMOTION_DIR` 환경변수, 없으면 저장소의 `remotion/`.
에피소드 위치는 `EPISODES_DIR` 환경변수, 없으면 저장소의 `episodes/`.

## 자주 걸리는 것

- **ComfyUI 확인은 TCP로.** ping 이 막혀 있어도 서버는 살아 있을 수 있습니다.
- **whisper 는 로컬 CPU 에서 돕니다.** 17씬 검사에 4~6분. 백그라운드로 돌리고 다른 일을 하세요.
- **긴 검사·렌더는 백그라운드로.** 포그라운드에서 기다리지 마세요.
- **자막 안전영역**: 화면 하단 56px 부터 자막이 옵니다. 도식 문구·캡션은 상단에 두세요.
- **ffmpeg 에서 트리밍과 패딩을 한 명령에 합치면** 패딩이 조용히 실패합니다. 두 번에 나누세요.
- **비싼 생성은 하나씩** 하고 매번 확인한 뒤 다음으로 갑니다.
- **검사기도 틀립니다.** 받아쓰기 오탐은 `whisper_fixes.json` 에 추가하고, 꼬리 잡음과 문장 누락을 구분하세요.

## 여섯 사람으로 나눠서 만든다

한 에이전트가 대본·화면·소리·도구를 다 하면 만든 사람이 자기 것을 검사하게 되고,
같은 실수가 반복된다. 그래서 여섯으로 나누고 총괄이 관문을 지킨다.

**다섯은 만들고 하나는 본다.** 앞의 다섯은 각자 재는 잣대가 있는데,
E01 에서 사용자가 찾은 셋(늘어난 얼굴 · 뜻이 안 전해지는 낱말 · 그냥 멎는 끝)을
**다섯이 다 통과시켰다.** 뒤로 갈수록 잴 수 없는 것이고 마지막 하나는 볼 자리가 없었다.

**나누는 것은 검증이지 손이 아니다.** 각 담당은 자기 결과물을 판단부터 코드까지
끝까지 쥔다. 넘기는 것은 남의 파일과 인프라뿐이다.

| 사람 | 정의 | 맡는 것 |
|---|---|---|
| 시나리오 작가 | `.claude/agents/screenwriter.md` | 주제·대본·씬 분할·강조 표시 |
| 연출 감독 | `.claude/agents/director.md` | 문법 선택·카드 배정·예외·흐름·편별 scenes.tsx |
| 미술 감독 | `.claude/agents/art-director.md` | 문법 값·테마 토큰·카드 부품 코드·소재 |
| 음악 감독 | `.claude/agents/sound-director.md` | 음성·읽기 사전·자막 데이터·음량 |
| 엔지니어 | `.claude/agents/engineer.md` | 파이프라인 스크립트·Remotion 조립 계층·렌더 설정·자동화·저장소·**서버와 연결·설치·환경** |
| 최종 편집자 | `.claude/agents/final-editor.md` | 완성본을 **보는 사람의 눈으로** 검수. 시각과 함께 낸다. **아무것도 만들지 않는다** |
| 총괄 | 지금 이 세션 | 배분·관문·보고 대조·읽기와 조회. **아무것도 만들지 않는다** |

### 자기 영역은 판단부터 코드까지 그 역할이 한다

**선 긋는 기준은 "누구의 결과물인가"다.** 연출이 "s06 은 표 말고 목록"이라고
정하는 것도 그것을 `scenes.tsx` 에 쓰는 것도 연출의 결과물이다.
판단과 구현을 사람으로 갈랐더니 값 하나 바꾸는 데 다섯 홉이 들었고,
그 왕복에서 잡힌 결함은 없었다. **한 결과물은 한 사람이 끝까지 쥔다.**

| 역할 | 자기가 쥐는 것 (판단 + 파일) |
|---|---|
| 시나리오 작가 | 주제, 대본 텍스트(`script/script_v1.md`), 씬 분할, 강조 표시 |
| 연출 감독 | 문법 선택, 씬별 카드, 예외, 리듬 — 그리고 **편별 `remotion/src/<ep>/scenes.tsx`** |
| 미술 감독 | 미술 판단 — 그리고 **`knowhow/` 의 토큰·문법·카드 부품** (`theme.ts` `grammars.json` `TextCard` `PromptCard` `ImageCard` `SplitCompare` `Workshop` `WhiteboardClip` `figures` `Container` `Stage` `Captions` `Thumbnail` `motion` `enter`) |
| 음악 감독 | 읽기 사전(`tts_overrides.json`), 음성·자막 판단, 음량 기준, `whisper_fixes.json` |
| 엔지니어 | `pipeline/` 전부, **조립·배선 계층**(`Root.tsx` `index.ts` `Episode.tsx` `grammar.ts` `captionRegistry.ts`), 렌더 설정, 자동화, 에이전트 정의, **서버·연결·설치·환경**, 저장소·커밋 |
| 최종 편집자 | 완성본을 보고 **어디가 걸리는지**. 아무것도 만들지 않는다 |
| 총괄 | 일 나누기, 검증 배정, 보고 대조, 사용자 보고 |

- **자기 파일이 아니면 고치지 않고 요청한다.** 미술이 `scenes.tsx` 를 고치지 않고,
  연출이 카드 부품을 고치지 않는다. 요청에는 무엇이 필요한지까지 쓰고 구현 방법은 지시하지 않는다
- **파이프라인 스크립트는 담당이 돌리고 엔지니어가 고친다.** 25개 1500줄과 조립 계층은
  그 자체로 엔지니어링이고, E01 에서 여기서만 여섯 건이 났는데 아무도 검사하지 않았다
- **서버·연결·설치·환경에 부딪히면 거기서 멈추고 증상만 넘긴다.** 어느 역할이든
  스스로 진단하지 않는다 — 포트를 훑거나 주소를 역추적하지 않는다
- 엔지니어는 받은 요구사항이 미술적·연출적으로 옳은지 판단하지 않는다

### 총괄 규칙

- **총괄은 만들지 않는다.** 대본·화면·부품·소리·스크립트·문서 어느 것도
  총괄이 새로 쓰거나 고치지 않는다. "한 줄이니까"도 예외가 아니다
- **읽기·조회·정해진 스크립트 실행은 총괄이 직접 한다.** 파일 확인, 상태 조회,
  숫자 대조, `01_status.py` · `05` · `07` · `85_ship_gate.py` 같은 판단 없는 실행은
  담당을 띄우지 않는다. 만들지 않으므로 「만든 사람이 검사한다」가 생기지 않는다
- **서버·연결·설치는 여전히 엔지니어 것이다.** 총괄도 연결 진단 명령을 직접 돌리지 않는다
- **누구 일인지 모르면 총괄이 처리하지 말고 사용자에게 승인을 요청한다.**
  "이 일은 <담당>의 것으로 보입니다. 그 담당에게 시킬까요?" 하고 답을 기다린다

### 검증 규칙 — 여기는 줄이지 않는다

이 구조가 값을 하는 자리는 검증 하나다. 왕복을 줄인 만큼 여기는 그대로 둔다.

- **만든 담당이 자기 결과를 검증하지 않는다.** 다른 담당이 무엇과 대조할지 스스로 정해서 잰다
- **바뀌기 전 산출물과 대조한다.** 고치기 **전에** 이전 판본을 떠 두고 고친 뒤 그것과 잰다.
  고친 것끼리 비교한 숫자는 검증이 아니라 재현이다
- **완성본은 최종 편집자가 본다.** 다섯이 각자 재는 잣대로는 안 잡히는 것이 있다

경계는 산출물로 긋는다. 남의 파일을 고치지 않고 요청한다.
교차 검토는 인접한 쪽끼리만, 한 가지만 본다.
"적용했다"는 숫자 없이 말하지 않는다. → `docs/AGENTS.md`
